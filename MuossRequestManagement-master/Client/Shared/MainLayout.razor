@inherits LayoutComponentBase

<MudThemeProvider @bind-IsDarkMode="@_isDarkMode" Theme="MyCustomTheme" />
<MudDialogProvider />
<MudSnackbarProvider />

@*https://stackoverflow.com/questions/58235005/blazor-template-with-menu-across-the-top*@
<MudLayout>
	@*<MudAppBar Elevation="1">
		<MudIconButton Icon="@Icons.Material.Filled.Menu" Color="Color.Inherit" Edge="Edge.Start" OnClick="@((e) => DrawerToggle())" />
		<MudText Typo="Typo.h5" Class="ml-3">Application</MudText>

		<MudSpacer />
		<CultureSelector />
		<MudToggleIconButton @bind-Toggled="@_isDarkMode" ToggledIcon="@Icons.Material.Filled.DarkMode" Icon="@Icons.Material.Filled.LightMode" Color="Color.Inherit" />
		<MudMenu FullWidth="true" AnchorOrigin="Origin.BottomCenter" TransformOrigin="Origin.TopCenter">

			<ActivatorContent>
				<MudChip Icon="@Icons.Material.Filled.Person" Color="Color.Primary">Account</MudChip>
			</ActivatorContent>
			<ChildContent>
				<MudMenuItem>Profile</MudMenuItem>
				<MudMenuItem>Theme</MudMenuItem>
				<MudMenuItem>Usage</MudMenuItem>
				<MudMenuItem>Sign Out</MudMenuItem>
			</ChildContent>
		</MudMenu>
	</MudAppBar>*@
	<header>
		<NavMenu @bind-isDarkMode="@_isDarkMode"/>
	</header>
@*	<MudDrawer @bind-Open="_drawerOpen" ClipMode="DrawerClipMode.Always" Elevation="2">
		<MudDrawerHeader>
			<MudText Typo="Typo.h6">Navigation</MudText>
		</MudDrawerHeader>
		<NavMenu />
	</MudDrawer>*@
	<MudMainContent Style="padding: 0;">
		<MudContainer MaxWidth="MaxWidth.ExtraLarge" Class="my-3 pt-3">
				@Body		
		</MudContainer>
	</MudMainContent>
</MudLayout>

@code {
	bool _drawerOpen = true;

	void DrawerToggle()
	{
		_drawerOpen = !_drawerOpen;
	}

	private bool _isDarkMode;

	MudTheme MyCustomTheme = new MudTheme()
		{
			Palette = new Palette()
			{
				Primary = "#0035AD",
				Secondary = "#FFC726"
				
			},
			PaletteDark = new PaletteDark()
			{
				Primary = Colors.LightBlue.Lighten1,
				Secondary = "#FFC726"
			},
		};
}